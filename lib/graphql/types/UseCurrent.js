export default {
  description: 'Objects set to use their current version unless overridden by a draft or override',
  fields: {
    type: { type: 'String', nullable: false },
    id: { type: 'String', nullable: false }
  }
}
